# Long-cache images only. They are renamed when changed, so this is safe.
# CSS/HTML intentionally left on Netlify's default so edits go live immediately.
/images/*
  Cache-Control: public, max-age=31536000
